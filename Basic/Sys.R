

ls() # check envi	var
str()	# extract info on object
example(func)	# func's example
args(func) # check func's parameters
get()	# Call an R object using a character string.
getwd( )	# show working dir
objects( )	# show objects in current dir
sessionInfo( )
gc()	# garbage collection
rm(A) # remove A
rm(list=ls())  # remove all objects

















